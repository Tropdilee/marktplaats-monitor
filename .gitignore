# Virtualenv: recreated by start.sh
.venv/
venv/

# Python
__pycache__/
*.py[cod]

# Everything the app writes while running: seen listings, the category cache,
# cached images, your own saved lists and the self-test report. All of it is
# generated or personal, and the app recreates the folder when it starts.
data/

# Loose notes and exports
*.log

# PyInstaller build folders
build/
dist/
.venv-build/
*.spec.bak
